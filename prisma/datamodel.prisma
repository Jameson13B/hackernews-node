type Link {
  id: ID! @unique @id
  createdAt: DateTime! @createdAt
  description: String!
  url: String! @unique
  postedBy: User
}

type User {
  id: ID! @unique @id
  name: String!
  email: String! @unique
  password: String!
  links: [Link!]!
}